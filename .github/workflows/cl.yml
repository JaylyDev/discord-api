name: cl

on: [push]

jobs:
  scripts:
    name: debug
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm install

      - name: build
        run: npx hereby
